# Avance del 26/08/2026

Cuatro commits en `main` (todos vía PR con squash). Orden cronológico.

| Hora  | Commit | PR | Alcance |
|-------|--------|----|---------|
| 12:17 | `35bb3da` | #39 | 13 archivos, +2451/-2 |
| 13:33 | `8263738` | #40 | 3 archivos, +67/-25 |
| 14:25 | `58f02b7` | #41 | 126 archivos, +9309/-1029 |
| 16:24 | `fbd5cd0` | #43 | 1 archivo, +122 |

---

## 12:17 · `35bb3da` — Mantenimiento suelto (#39)

- **`.gitignore`**: la regla era el nombre exacto `application-secrets.yaml`, así que no cubría `application-secrets.yaml.bak-antes-de-quitar-amazon-mq` (seis líneas de credenciales, sin seguir). Pasa a `application-secrets.yaml.*`. El archivo salió del repositorio.
- Se ignoran dos salidas de scripts que estaban en la raíz: el xlsx (lleva dentro respuestas de candidatos reales) y el pdf de la prueba técnica. Ninguno es fuente.
- **`excel-de-la-prueba.py`** numeraba por el campo `orden` del catálogo del banco, no por el de la plantilla (ADMIN_Q01 salía con un 7). Ahora numera por posición en la lista, que es el orden en que se rindió.
- Se sube el diagrama de la IA y los gates (escrito el 24/08, nunca versionado) y la skill de brainstorming.

## 13:33 · `8263738` — Docs del perfil sin comodines (#40)

Los documentos describían las cinco listas del perfil con un patrón («mismo patrón para las cinco») que el código no cumple. Se sustituye por una tabla explícita, fila por lista.

Falsedades corregidas, todas verificadas contra código:
- Enlaces solo tiene POST y DELETE; reordenar es solo de experiencia y educación.
- Los catálogos **no** devuelven `orden` (`OpcionCatalogo` es `codigo, nombre`).
- El `tipo` de enlace es la única excepción al «no escribas valores a mano»: viven en un `List.of()` del servicio.
- Hay **dos** formas de 400: `IllegalArgumentException` (solo `detail`) y `MethodArgumentNotValidException` (además `errors`, mapa campo → mensaje).
- Swagger está en el **8081**, no en el 8080 (ahí suele estar Adminer, que responde 200 a todo).
- El `@Operation` de `PUT /perfil` omitía `experienciaMeses` y que reemplaza la cabecera entera.

## 14:25 · `58f02b7` — Multiempresa, fase 1 (#41)

El grueso del día. Seis piezas de diseño (A–F) y tres implementadas.

**Diseño (specs A–F)**: reparto empresa/Renaser con cuatro banderas de personalización; aislamiento como garantía (lo ajeno es 404); identidad y alta por invitación; el candidato ante varias empresas; coste de la IA con tope mensual (aviso al 80, freno al 100, sin romper procesos en marcha); Renaser administra el continente, no el contenido.

**V37 + V38 (esquema)**: `es_plataforma` con índice parcial (solo una organización puede serlo); cuatro banderas por empresa; `es_equipo` como discriminador de login; tabla de invitaciones de un solo uso (patrón de `enlace_acceso`); el banco pierde el modelo «NULL = biblioteca global»; `copiada_de_version_id`; permisos `personalizar_instrumentos` y `administrar_plataforma`. V38 amarra el consentimiento a la postulación y trae el coste de IA; V39 siembra la tarifa del modelo rápido.

**Implementación**:
- `POST /panel/auth/login` con correo y contraseña (RENASER OS queda dormido). El filtro `es_equipo` es la línea entera entre los dos mundos. Dev-login **apagado por defecto**.
- Alta de empresa (`POST /panel/plataforma/empresas`): roles, permisos, parámetros, textos legales en borrador, correos activos e invitación del primer administrador.
- `DuenoDelInstrumento`: único punto donde se interpreta una bandera. Encender = copiar en transacción (`CopiadorDeInstrumentos`); apagar archiva, no borra (RF-138).
- **Seis fugas entre empresas cerradas**: borrado 29733 pasa a la plataforma, inscripción a sesión, versión completa de prueba, barrera crítica, plantilla de prueba en vacante, alerta de pregunta.
- El portal se abre a todas: el tablón lista vacantes publicadas de todas las empresas y la postulación nace en la organización **de la vacante**.
- **Dos vigilantes**: regla ArchUnit (nada de `findById` sobre agregados con dueño salvo excepciones enumeradas) y `FlujoDosEmpresasIT`, que recorre el viaje entero incluido rendir los 50 ítems del banco de Renaser en una vacante de ACME.

Pruebas: **682 en verde** (589 unitarias + 93 de integración), contadas por elementos `<testcase>`.

## 16:24 · `fbd5cd0` — Prueba anti-colisión de migraciones (#43)

El único conflicto que git no sabe ver: dos ramas con `V40__una_cosa.sql` y `V40__otra_cosa.sql` son dos archivos distintos y se fusionan sin marcar conflicto. Pasó hoy mismo con tres ramas reclamando el 37.

Tres comprobaciones sin Docker: números repetidos (nombra a las culpables), formato de nombre que Flyway espera y numeración sin huecos.

⚠️ **No basta por sí sola**: cada rama pasa el CI en verde por separado. Para que salte antes del merge hay que exigir en GitHub que la rama esté al día con `main`.

---

## Para mañana

- Exigir en GitHub «rama al día con main» para que la prueba de migraciones muerda antes del merge (lo pone quien tenga permisos de administrador en GitHub).
- Multiempresa: A, B, C y D implementadas; **E (coste de IA, solo el esquema) y F** siguen en diseño.
- `es_activa` no se comprueba al iniciar sesión (hueco anotado en la pieza F).
